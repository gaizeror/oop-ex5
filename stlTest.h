#include <iostream>
#include <string.h> // change to <string> for visual studio. don't forget to change it back before you submit
#include <vector>
#include <list>
#include <algorithm>

using namespace std;

template<______________________________________________________>
void filter(________________________________________________________________){
	________________________________________________________________
	________________________________________________________________
	________________________________________________________________
}

struct Even{
	________________________________________________________________
	________________________________________________________________
	________________________________________________________________
	________________________________________________________________
};
struct PrintMe{
	__________________________________________________________________
	__________________________________________________________________
	__________________________________________________________________
	__________________________________________________________________
	__________________________________________________________________
	__________________________________________________________________
	__________________________________________________________________
	__________________________________________________________________
};


class Sum{
	T sum;
public:
	__________________________________________________________________
	__________________________________________________________________
	__________________________________________________________________
	__________________________________________________________________
	__________________________________________________________________
	__________________________________________________________________
	__________________________________________________________________
	__________________________________________________________________
};

int mainAPISTL()
{
	vector<int> ints;
	for (int i = 0; i<10; i++)
		ints.push_back(i);

	list<int> evenInts;
	filter(ints.begin(), ints.end(), Even(), evenInts);
	for_each(evenInts.begin(), evenInts.end(), PrintMe(",")); // 0,2,4,6,8,
	cout << endl;

	Sum<int> sum;
	sum = for_each(evenInts.begin(), evenInts.end(), sum);
	cout << sum.get() << endl; // 20
	return 0;
}